% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OrdinalSTAAR.R
\name{OrdinalSTAAR}
\alias{OrdinalSTAAR}
\title{OrdinalSTAAR procedure using omnibus test}
\usage{
OrdinalSTAAR(
  Geno,
  MAF = NULL,
  MAC = NULL,
  objNull,
  annotation_phred = NULL,
  rare_maf_cutoff = 0.01,
  rare_num_cutoff = 2,
  combine_ultra_rare = TRUE,
  ultra_rare_mac_cutoff = 20,
  use_SPA = NULL,
  SPA_filter = TRUE,
  SPA_filter_cutoff = 0.05,
  verbose = FALSE
)
}
\arguments{
\item{Geno}{n x p 剂量基因型矩阵（行：样本，列：变异）。}

\item{MAF}{可选，长度 p 的小等位基因频率向量；NULL 时由 \code{genoFlip()} 计算。}

\item{MAC}{可选，长度 p 的小等位基因计数；NULL 时由 \code{genoFlip()} / MAF 衍生。}

\item{objNull}{\code{OrdinalSTAAR_NullModel()} 的输出对象。}

\item{annotation_phred}{p x q 的功能注释（Phred 分数；或长度 p 的向量）。允许 q=0。}

\item{rare_maf_cutoff}{稀有阈值（默认 0.01）。}

\item{rare_num_cutoff}{进入集合检验的最少变异数（默认 2）。}

\item{combine_ultra_rare}{是否在失衡情形下合并超稀有变异用于 SKAT/ACAT-V（默认 TRUE）。}

\item{ultra_rare_mac_cutoff}{超稀有 MAC 阈值（默认 20）。}

\item{use_SPA}{是否启用 SPA（默认沿用 \code{objNull$use_SPA}）。}

\item{SPA_filter}{仅当单变异 P 值小于阈值时用 SPA 重算（默认 TRUE）。}

\item{SPA_filter_cutoff}{SPA 重算阈值（默认 0.05）。}

\item{verbose}{是否打印进度（默认 FALSE）。}
}
\value{
列表：包含 OrdinalSTAAR-O 结果与集合的基本统计（变异数、cMAC、MAF）。
}
\description{
\code{OrdinalSTAAR} 对目标变异集合执行基于有序结局的 STAAR-O 全面检验。
依赖：已拟合的 \code{OrdinalSTAAR_NullModel()}（固定效应比例优势模型，附带 SPA 的经验 CGF）。
}
